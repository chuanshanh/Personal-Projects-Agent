# V0.1 审查与验收记录

审查日期：2026-09-09。以 PROJECT.md 为准，保留 Streamlit + SQLite + 单 Agent 原结构。

## 结论

基础功能可运行，Definition of MVP Done 中除手机实际操作外均已有验收证据。DeepSeek 真实模型自然语言闭环已通过。完成手机验收后进入真实使用，不继续扩展功能。

## 本轮按优先级修复

| 优先级 | 审查发现 | 处理与证据 |
| --- | --- | --- |
| P1 数据正确性 | 旧复选框状态可在页面重跑时覆盖外部/Agent 新状态 | 数据库状态用于渲染，只有实际点击事件回写；测试覆盖完成与重新打开后的刷新 |
| P1 数据正确性 | SQLite 删除后复用 ID，旧对话可能误操作新建对象 | 项目与任务使用 AUTOINCREMENT；旧库升级前备份，事务迁移保留字段和 ID；测试覆盖数据、级联关系与失效 ID |
| P1 手机认知负担 | Today 展开所有项目和到期任务，未按优先级排序 | 默认 3 项目 + 3 任务，其他任务折叠；高优先级优先，显示所属项目；扩大勾选区域 |
| P1 验收证据 | 仅验证少量手动交互，没有失败后状态刷新测试 | 增加任务创建、编辑、删除、外部状态同步、聊天成功和部分失败后的刷新测试 |
| P2 工具缺口 | PROJECT.md 指定的 update_project / delete_project 未暴露 | 增加两个真实 SQLite 工具，SDK 调度测试覆盖暂停及级联删除 |
| P2 配置缺口 | 只有 OpenAI Key / model，不能独立配置 provider / base URL | 增加四项 LLM 配置，保留旧配置兼容；实际 SDK Chat Completions 适配器通过模拟 HTTP 工具调用测试 |
| P2 项目信息 | 项目状态藏在编辑区 | Projects 详情直接显示中文状态 |

未新增 Not Now 功能，Explore 仍为预留入口。没有新增登录、多 Agent、部署框架或数据库服务。

## 已验证

- `.venv/Scripts/python -m pytest -q`：13 项通过。
- 项目与任务 CRUD、参数校验、完成时间、进度与级联删除。
- 新 Python 进程重新打开数据库后保留修改；SQLite 备份可重新打开。
- 旧数据库升级保留数据，删除后的 ID 不复用。
- Streamlit AppTest：三页面、项目创建、任务创建/编辑/完成/删除、今日任务排序与数量、数据库外部修改后的同步。
- 脚本化模型 → 真实 Agents SDK Runner → 9 个工具 → SQLite。
- 模拟供应商 HTTP 响应 → 真实 Chat Completions 适配器 → 完成任务工具 → SQLite。
- 本地服务 `/_stcore/health` 返回 `ok`。

## 仍待完成（阻止勾选全部 Done）

### 1. 手机网页实际操作

自动浏览器的 390×844 视口检查因审批服务报告用量上限而被拒绝，没有取得截图或实机证据。AppTest 不验证触摸、布局溢出或 Safari 键盘行为。

在同一 Wi-Fi 的手机 Safari 打开运行中的网页，依次检查：

1. Today 的导航、任务名、进度不横向溢出；前三项可快速查看。
2. 创建项目，新增任务，修改标题、优先级与日期。
3. 勾选任务后状态与进度立即变化；再打开项目仍然正确。
4. 删除一条测试任务后消失，刷新后不复现。
5. 配置模型后，输入框在软键盘出现时仍能输入、发送、查看结果。

部署仍沿用 README：本机持续运行可供局域网手机使用；Community Cloud 本地 SQLite 不视为可靠长期存储。未主动迁移基础设施。

### 2. 真实自然语言 Agent 验收（已通过）

2026-09-09 使用已配置的 DeepSeek API 完整运行：

在环境变量或本地 `.streamlit/secrets.toml` 配置四个 `LLM_*` 值后运行：

```powershell
.venv/Scripts/python scripts/live_smoke.py
```

脚本使用临时数据库完成：原始模糊建项目请求 → 三个任务真实落库 → 动态建立第三项未来截止日期 → “第二个完成，第三个推迟一下” → 第二项完成 → 第三项从 2026-09-16 顺延为 2026-09-17 → 回复包含数据库完整日期且没有错误的“明天/后天”称呼 → 删除第三项 → 重新读库剩余两项。最终输出 `PASS: real model -> SDK tools -> SQLite -> persisted state`。

为消除模型依据旧聊天日期计算的问题，新增 `postpone_task`：顺延天数由 Agent 提供，基准日期由数据库当前状态决定。未来任务从原日期顺延；未排期或已到期任务从今天顺延。

## 真实使用记录

尚未记录真实使用反馈。后续只在此补充反复出现的 Pain Points（场景、实际阻碍、出现次数）；不自动扩展为新功能。
